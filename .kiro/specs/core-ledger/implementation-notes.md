# Implementation Notes

`.kiro/specs/core-ledger/` is the intentional Kiro-native spec location for this request. Do not create or migrate this mission to another directory.

The design and tasks use the source reconnaissance/RCA package at `.specship/artifacts/reverse-engineering/` as their evidence base, including the architecture recommendation, change-impact map, API contracts, data models, preserved behaviors, and test baseline.
