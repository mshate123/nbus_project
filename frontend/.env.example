# Frontend environment variables
# VITE_API_BASE_URL=http://localhost:8000

# API base path — defaults to "/api" (proxied by nginx in Docker).
# For local non-Docker development, set to your backend's URL, e.g.:
# VITE_API_BASE=http://localhost:8000/api
VITE_API_BASE=/api
